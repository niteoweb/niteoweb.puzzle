<?xml version="1.0" encoding="UTF-8"?>
<rules
    xmlns="http://namespaces.plone.org/diazo"
    xmlns:css="http://namespaces.plone.org/diazo/css"
    xmlns:xsl="http://www.w3.org/1999/XSL/Transform">

    <!-- change header ID for the one that is used on subpages -->

    <!-- change id="header-index" with id="header-subpage"

       also remove class "section-front-page" from body tag
    -->

    <!--<replace css:theme="#header-index" css:content="#header-index" />-->

<!--    <xsl:template match="#header-index">
      <xsl:attribute name="id">
        <xsl:value-of select="header-subpage" />
      </xsl:attribute>
    </xsl:template>-->

<!--    <replace css:theme-children="h1#page-title">
      Web presence,<br/> made easy.
    </replace>
    <replace css:theme-children="p#slogan">
      We put the pieces together.
    </replace>-->


    <!-- copy portal breadcrumbs -->
    <replace css:theme-children="#breadcrumbs-navig" css:content-children="#portal-breadcrumbs"/>

    <!-- copy all content into theme -->
    <replace css:content-children="#content" css:theme-children="#main-content"/>

    <drop css:content="h1.documentFirstHeading"/>

    <replace css:content="#parent-fieldname-description">
      <p>
        <strong><xsl:copy-of select="//*[@id='parent-fieldname-description']/text()"/></strong>
      </p>
    </replace>

</rules>